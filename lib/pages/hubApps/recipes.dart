import 'package:flutter/material.dart';

class RecipeCreatorPage extends StatelessWidget {
  const RecipeCreatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contenu du Créateur de Recettes'),
    );
  }
}